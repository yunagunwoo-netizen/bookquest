# 북퀘스트 세션 인계서 (북퀘스트_2 → Opus 4.7 새 세션)

**작성일**: 2026-04-21
**이전 세션**: `북퀘스트_2` (Opus 4.6)
**다음 세션**: Opus 4.7

---

## 첫 메시지로 이것부터 읽어줘

새 세션을 시작했으면 다음 파일들을 순서대로 Read:

1. `C:\Users\ggyeo\Downloads\건우독서앱\BookQuest_프로젝트_컨텍스트.md` — 프로젝트 전체 컨텍스트 (최신 업데이트 반영)
2. `C:\Users\ggyeo\Downloads\건우독서앱\북퀘스트_세션인계서.md` — 이 파일

---

## 북퀘스트_2 세션에서 끝낸 작업 요약

### ✅ 완료한 것
1. **대시(Dash) 오리지널 캐릭터 추가** — `AVATAR_DATA`, `COMBO_AVATARS`, `getComboImagePath` 수정
   - 콤보 이미지 외부 파일 폴백 시스템 구축 (`EXTERNAL_COMBO_AVATARS` 도입)
   - `avatars/combos/{avatarId}_{itemId}.png` 규칙
2. **관리자 모드** — PIN `1234`, 프로필 탭 하단 "🔒 관리자 모드" 버튼
   - 콘텐츠 관리 / 유저 관리 / 앱 설정 3개 탭
3. **용량 정리** — 1.3GB+ → 46MB (콤보 이미지 200×200 리사이즈, PDF·원본 이미지 `.gitignore` 처리)

### ⚠️ 작업 중 있었던 사고
- index.html 편집 중 ChangelogModal 부분이 잘리는 사고 발생 → 백업 파일(`index_brawlstars_backup.html`)에서 복원 후 변경사항 재적용 완료
- **교훈**: 대규모 편집 시 반드시 백업 먼저 + 큰 수정은 Agent 도구 활용

---

## 🎯 새 세션에서 이어서 할 수 있는 작업

### A. 배포 확인 (가장 급한 것)
사용자가 push하려던 상태에서 끝남. 현재 필요한 단계:
```bash
cd C:\Users\ggyeo\Downloads\건우독서앱
git status
git add .
git commit -m "Add Dash character + admin panel + resize combo images"
git push
```
그 후 https://yunagunwoo-netizen.github.io/bookquest/ 에서 정상 동작 확인.

### B. QA (실기기/브라우저 테스트)
- 대시 캐릭터 선택 → 아이템 장착 시 콤보 이미지 정상 렌더링 확인
- 관리자 모드 PIN 로그인 정상 동작 확인
- 기존 브롤스타즈 5캐릭터 영향 없는지 회귀 테스트

### C. 다음 오리지널 캐릭터 추가
남은 9캐릭터: **벅스(토끼), 제로(늑대), 너츠(다람쥐), 버디(독수리), 무진(곰), 몽키(원숭이), 아스(사슴), 섀도(표범), 릭스(도마뱀)**

각 캐릭터당:
1. 나노바나나에서 아바타 + 콤보 이미지 30장 생성
2. `avatars/{id}.png` (200×200) + `avatars/{id}_sm.png` (60×60) 저장
3. `avatars/combos/{id}_{itemId}.png` 30장 저장 (200×200)
4. `AVATAR_DATA`에 추가
5. `COMBO_AVATARS`에 ID 추가 → 자동으로 외부 파일 경로 사용

### D. 10캐릭터 모두 완성 후 전환
- `rollback.sh original` 실행 → 오리지널 선생님(루미/미오/노아)까지 교체
- Firebase 함수도 오리지널 버전으로 배포

---

## 핵심 파일 위치

- 메인: `C:\Users\ggyeo\Downloads\건우독서앱\index.html`
- 백업: `index_brawlstars_backup.html`, `index_original_backup.html`
- 백엔드: `functions/index.js`
- 롤백 스크립트: `rollback.sh`

---

## 주의사항

- **index.html은 단일 파일에 ~3,700줄**이라 대규모 수정 시 꼭 백업 확인
- 콤보 이미지는 반드시 **200×200 이하**로 저장 (GitHub Pages 용량 관리)
- `.gitignore`에 `*.pdf`, `_원본이미지/`, `오리지날 캐릭터/` 이미 설정됨 — 임의로 커밋에 포함시키지 말 것
